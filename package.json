{
  "name": "authencation-service",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "rebuild": "docker stop $(docker ps -aq) && docker rm -vf $(docker ps -aq) && docker rmi -f $(docker images -aq) && docker-compose up -d"
  },
  "license": "MIT"
}
